name: Gradle Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-build.yml@v2.3.0
    with:
      flow-type: public
      java-version: '21'
